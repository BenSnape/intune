# Install the Windows Configuration Designer to a Windows 10 PC for the company
# https://docs.microsoft.com/en-us/windows/configuration/provisioning-packages/provisioning-install-icd
# (you can use your own but it will be associated with the company)
# Create a new Windows 10 deployment
# Add the computername as the Windows Enrollment Profile i.e. Company-%SERIAL%
# Setup a wireless network if known
# Enroll a bulk token, the device is added t the company. the token will last 6 months
# Add applicaitions if needed
# Create
# Remove account from settings if not a company machine
# Rebuild a PC with the latest Windows 10 build
# https://www.microsoft.com/en-gb/software-download/windows10
# Insert the USB or copy the settings to the Win 10 USB
# https://docs.microsoft.com/en-us/windows/configuration/provisioning-packages/provisioning-apply-package
